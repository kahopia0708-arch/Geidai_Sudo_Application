namespace Geidai.Foundation
{
    /// <summary>
    /// 登録画面のモード（domain-entities.md / US-REG-01/02）。
    /// New=初回登録（空）、Edit=既存プロフィールの編集（初期値表示）。
    /// </summary>
    public enum RegistrationMode
    {
        New,
        Edit
    }
}
