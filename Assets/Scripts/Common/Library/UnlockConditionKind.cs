namespace Geidai.Common.Library
{
    /// <summary>解除条件種別（通貨・XP・ライフは持たない / BR-UNLOCK-05）。</summary>
    public enum UnlockConditionKind
    {
        GameClear = 0,
        RecordingChallenge = 1,
        Combined = 2
    }
}
